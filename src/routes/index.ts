import { userRoutes } from './user';
import { betRoutes } from './bet';
import { transRoutes } from './transaction';

export const Routes = {
  user: userRoutes,
  bet: betRoutes,
  trans: transRoutes
};
